---
name: Roadmap
route: /roadmap/
---

# Roadmap

Upcoming project development:
- Work on `unified-doc-cli` package.
- Work on `unified-doc-parse-pdf` (or equivalently `repdf`) package.
- Work on `unified-doc-parse-docx` (or equivalently `redocx`) package.
- Discuss and spec `.uni` file format (`text/uni`) with `unified` team.
