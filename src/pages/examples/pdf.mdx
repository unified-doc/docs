import PdfExample from '~/examples/pdf';

PDF documents are fundamentally incompatible with HTML/XML, but this is a hacky approach to representing PDFs as HTML images and text nodes, which still makes it possible to benefit from `unified-doc` APIs (e.g. render, search, export, marks).

<PdfExample />
