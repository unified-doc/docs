import PdfExample from '~/examples/pdf';
import { SourceCode } from '~/ui';

<SourceCode source="examples/pdf" />

This example converts PDF documents to HTML documents, represented by images and text nodes using [`pdf.js`][pdfjs].  With the PDF represented by a HTML document, we are now able to benefit from all `unified-doc` APIs (e.g. render, search, export, marks).

<PdfExample />

<!-- Definitions -->
[pdfjs]: https://github.com/mozilla/pdf.js
